# Copy to .env for LOCAL development only: cp .env.example .env
# DO NOT commit .env. Set these in MochaHost cPanel environment variables for hosting.
NODE_ENV=development
PORT=3000
TAKATAK_ADMIN_KEY=
WAVE_ACCESS_TOKEN=
