# LOCAL template only. Never commit .env, customer data, database URLs or credentials.
# Deploy these through ONLY the new facturations.bolon.ca application's private environment.
NODE_ENV=development
PORT=3000
TAKATAK_ADMIN_KEY=
WAVE_ACCESS_TOKEN=
# Both values below must be configured together to enable saving drafts.
# Use a NEW isolated PostgreSQL database, never the existing TAKATAK production DB.
FACTURATIONS_DATABASE_URL=
WAVE_BUSINESS_ID=
