{
  "name": "takatak-wave",
  "version": "0.5.3",
  "private": true,
  "description": "GROUPE TAKATAK — independent Wave connector, private drafts and staff authentication foundation",
  "main": "app.js",
  "type": "commonjs",
  "engines": { "node": ">=20.11.0" },
  "scripts": {
    "start": "node app.js",
    "dev": "node --env-file=.env app.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && for file in src/*.js tests/*.test.js scripts/*.js; do node --check \"$file\" || exit 1; done"
  },
  "dependencies": { "pg": "^8.13.0" },
  "license": "UNLICENSED"
}
